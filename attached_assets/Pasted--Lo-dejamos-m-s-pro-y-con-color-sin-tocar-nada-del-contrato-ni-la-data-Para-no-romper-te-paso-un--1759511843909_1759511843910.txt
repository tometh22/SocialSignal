¡Lo dejamos más pro y con color sin tocar nada del contrato ni la data!
Para no romper, te paso un “polish pack” de cambios puramente de UI. Son drop-in y no modifican el fetch, ni el shape del payload, ni el layout base.

Qué mejora (sin riesgos)

KPI cards con borde degradado y glass sutil.

Tarjetas de proyecto con ribbon lateral en degradé.

Color semántico para Markup/Margen (verde/ámbar/rojo).

Hovers suaves, sombras y tipografía más marcada.

Todo sigue en español y con tu filtro temporal actual.

Cambios de código (seguros)

Aplica estos reemplazos en tu componente de “Proyectos Activos”. No cambian props ni endpoints; solo clases/markup y un par de helpers.

1) KPI más pro (reemplaza solo la función KPICard)
function KPICard({ title, value, icon }: { title: string; value: string; icon: React.ReactNode }) {
  return (
    <div className="relative rounded-2xl p-[1px] bg-gradient-to-br from-indigo-200/70 via-violet-200/70 to-fuchsia-200/70">
      <div className="rounded-2xl bg-white/90 backdrop-blur px-4 py-3 shadow-sm flex items-center gap-3">
        <div className="rounded-xl bg-gradient-to-br from-indigo-500 to-violet-500 text-white p-2 shadow-sm">
          {icon}
        </div>
        <div>
          <div className="text-xs text-slate-500">{title}</div>
          <div className="text-xl font-semibold text-slate-900">{value}</div>
        </div>
      </div>
    </div>
  );
}

2) Stat con “tono” (reemplaza la función Stat y no toques las llamadas excepto las 2 que indico abajo)
function Stat({label, value, tone}:{label:string; value:string; tone?: 'default'|'good'|'warn'|'bad'}) {
  const color = tone==='good' ? 'text-emerald-700'
              : tone==='warn' ? 'text-amber-700'
              : tone==='bad'  ? 'text-rose-700'
              : 'text-slate-900';
  return (
    <div className="rounded-xl bg-slate-50 p-3 text-center">
      <div className="text-xs text-slate-500">{label}</div>
      <div className={`text-sm font-semibold ${color}`}>{value}</div>
    </div>
  );
}

3) Tono para Markup/Margen (agrega estas dos constantes donde calculás markup y margin)
const marginTone: 'good'|'warn'|'bad'|undefined =
  Number.isFinite(margin) ? (margin! < 0 ? 'bad' : margin! < 0.5 ? 'warn' : 'good') : undefined;

const markupTone: 'good'|'warn'|'bad'|undefined =
  Number.isFinite(markup) ? (markup! < 1 ? 'bad' : markup! < 2 ? 'warn' : 'good') : undefined;

4) Usa el “tono” en estas dos líneas (solo estas)
<Stat label="Markup" value={Number.isFinite(markup) ? `${markup.toFixed(1)}x` : "—"} tone={markupTone} />
<Stat label="Margen" value={Number.isFinite(margin) ? `${(margin*100).toFixed(1)}%` : "—"} tone={marginTone} />

5) Ribbon de color en cada card (añade un div dentro del motion.div al inicio)

En el motion.div contenedor de cada proyecto:

<motion.div ... className="relative overflow-hidden rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition-shadow hover:shadow-md">
  <div className="absolute inset-y-0 left-0 w-1 bg-gradient-to-b from-indigo-500 via-violet-500 to-fuchsia-500" />
  {/* ...resto del contenido */}
</motion.div>


Solo cambia la className de ese motion.div y agrega la línea del div del ribbon. El resto queda igual.

Bonus opcionales (0 riesgo, si querés sumar ahora mismo)

Dark mode: agrega a tu <html className="dark"> o respeta prefers-color-scheme y añade variantes dark: en 3–4 clases (puedo pasarte el patch).

Density toggle: un switch que cambia p-5 → p-3 en cards (compacto para data-heavy).

Barrita de margen: un progress fino debajo del valor (si querés, te paso el ProgressBar drop-in).

QA ultra-rápido

Visual: KPI con degradé, cards con ribbon y hovers suaves.

Semántica: Margen/Markup se pintan verde/ámbar/rojo según valor.

Nada funcional cambió: los mismos endpoints, filtros, búsqueda y “Actualizar” intactos.